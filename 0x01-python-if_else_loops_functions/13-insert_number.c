#include <stdio.h>
#include <stdlib.h>
#include "lists.h"
/**
 * insert_node - inserts a node
 * @head: head
 * @number: int to add in the node
 * Return: the address of the new node, or NULL if it failede
 */
listint_t *insert_node(listint_t **head, int number)
{

}